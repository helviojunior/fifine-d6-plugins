# Build output (regenerated by ./build.sh)
build/

# Vendored npm deps (installed by build.sh inside each plugin/)
node_modules/

# Cross-compiled launcher (regenerated by build.sh / build.ps1 into tools/)
tools/launch.exe

# StreamDock / CEF runtime debug output
debug.log

# macOS
.DS_Store
